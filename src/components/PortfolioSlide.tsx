import { type Component } from "solid-js";

const PortfolioSlide: Component<{title: string, img: string, content: string, ProjectLink?: string, SourceLink?: string}> = (props) => {
    return (
    <div>
        
    </div>
    )
}

export default PortfolioSlide;